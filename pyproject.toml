[project]
name = "door-pi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "parameterized>=0.9.0",
    "door-manager",
    "uc-firmware",
]

[tool.uv.workspace]
members = ["door_manager", "uc"]

[tool.uv.sources]
door-manager = { workspace = true }
uc-firmware = { workspace = true }
